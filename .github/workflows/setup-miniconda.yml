on: workflow_dispatch
jobs:
  run:
    runs-on: windows-latest
    steps:
    - uses: goanpeca/setup-miniconda@fix/incorrect-warning
      with:
        python-version: '3.10'
        miniforge-version: latest
        channels: conda-forge
        conda-remove-defaults: true
